```
uv sync
```

activate the virtual environment:

```
source .venv/bin/activate
```

windows :

```
.venv\Scripts\activate
```

Then you can run the project with:

```
python main.py
```

(linter a bit broken, ignore the warnings)
